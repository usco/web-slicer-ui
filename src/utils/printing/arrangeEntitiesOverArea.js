/**
 * tries to fit the maximum number of entities in the given area
 * it returns a new list of entities, with modified positions: it does NOT mutate the original data
 * @param  {array} entities list of entities to fit: they MUST have correct bounding boxes/spheres
 * @param  {array} area an array (or arraybuffer) of length 2, with width & length of the area
 * @return {array} entities the modified list of entities with updated positions/transforms
 */
function arrangeEntitiesOverArea (entities, area, params) {

}

module.exports = arrangeEntitiesOverArea
